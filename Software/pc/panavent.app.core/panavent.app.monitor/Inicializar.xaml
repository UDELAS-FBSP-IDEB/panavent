<Page
    x:Class="panavent.app.monitor.Inicializar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:panavent.app.monitor"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="#FF1A1A1A">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="100"></RowDefinition>
        </Grid.RowDefinitions>
        <TextBlock x:Name="tbErrorLog" Text="{Binding LogString, Mode=OneWay}" Foreground="White" Margin="25" />
        <StackPanel  Grid.Row="1" Margin="25" Orientation="Horizontal">
            <Button x:Name="btReintentar" IsEnabled="{Binding Reintentar}" Content="Reintentar"  HorizontalAlignment="Right" Tapped="btReintentar_Tapped"  />
            <Button x:Name="btContinuar" IsEnabled="{Binding Continuar}"  Content="Continuar"  HorizontalAlignment="Right" Tapped="btContinuar_Tapped"  />
        </StackPanel>
       
        
    </Grid>
</Page>
